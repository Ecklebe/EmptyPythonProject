image: python:3.6

stages:
  - mirror
  
variables:
  GITHUB_OAUTH_TOKEN: "$GITHUB_OAUTH_TOKEN"
  
github_mirror:
  stage: mirror
  only:
    - master
  script:
    - git checkout master
    - git pull origin master
    - git push git@github.com:Ecklebe/EmptyPythonProject.git master --force